function search(num,ind){
    let a ;
    for(let i=0 ; i<num.length;i++){
        // a = num[i]===ind ? i:-1
        // if(num[i]===ind){
        //     return i
        // }
    }
    return num.indexOf(ind)
}
console.log(search([1, 5, 3], 5) )

console.log(search([9, 8, 3], 3) )

console.log(search([1, 2, 3], 4) )