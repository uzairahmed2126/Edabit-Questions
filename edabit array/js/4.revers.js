function reverse(rev) {
    // it's process name is accumulate when we use the parameter it's called accumulate
    return (rev.reverse());

}

console.log(reverse([1, 2, 3, 4]))

console.log(reverse([9, 9, 2, 3, 4]))

console.log(reverse([]))